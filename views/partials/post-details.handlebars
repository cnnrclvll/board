<section class="post">
    <div class="post-body">
        <img src="{{source}}" alt="{{description}}">
        <div class="post-overlay">
          <h2>{{title}}</h2>
          <p>By: {{user.user_name}}</p>
        </div>
    </div>
  </a>
</section>